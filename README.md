# db-migration-masking-ai
Database-agnostic, AI-assisted Spring Boot engine that migrates data between any source/destination pair (Postgres, MySQL, Oracle, SQL Server, MongoDB) while irreversibly masking                    ▎ GDPR/HIPAA/PCI-DSS-scoped columns, preserving referential integrity, and applying additive-only destination DDL.
